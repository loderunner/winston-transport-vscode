version: 2.1

orbs:
  node: circleci/node@5.2.0

workflows:
  Lint-test-build:
    jobs:
      - node/run:
          npm-run: lint
      - node/test:
          test-results-for: jest
      - node/run:
          npm-run: build
